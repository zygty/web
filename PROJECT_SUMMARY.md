# 航空轨迹监控系统 - 项目总结

> **课程作业**：航空地图的设计与实现  
> **完成时间**：2026年6月  
> **技术栈**：Hugo, Leaflet, Python/PyTorch, Flask  
> **项目地址**：http://localhost:1313/web/

---

## 一、项目概述

### 1.1 项目背景

本项目旨在构建一个**航空轨迹可视化与预测系统**，能够：
- 在交互式地图上实时展示航空器轨迹
- 基于历史数据预测未来飞行路径
- 提供时间轴播放、数据质量监控等高级功能

### 1.2 核心目标

1. **数据可视化**：将ADS-B（广播式自动相关监视）数据在地图上动态展示
2. **轨迹预测**：使用LSTM深度学习模型预测飞机未来位置
3. **实时交互**：提供时间轴控制、播放/暂停、速度调节等功能
4. **数据质量监控**：实时显示航班数量、数据状态、轨迹点数等信息

---

## 二、技术栈

### 2.1 前端技术

| 技术 | 版本 | 用途 |
|---|---|---|---|
| **Hugo** | v0.159.0 | 静态网站生成器 |
| **Leaflet** | v1.9.4 | 交互式地图库 |
| **JavaScript (ES6+)** | - | 前端逻辑 |
| **CSS3** | - | 样式设计 |

### 2.2 后端技术

| 技术 | 版本 | 用途 |
|---|---|---|---|
| **Python** | 3.10+ | 后端开发 |
| **PyTorch** | 2.6.0 | 深度学习框架 |
| **Flask** | - | REST API服务 |
| **pandas** | 2.3.3 | 数据处理 |

### 2.3 开发环境

- **本地开发**：macOS + VSCode + Hugo Server
- **远程训练**：Linux (Ubuntu) + NVIDIA RTX 4060 Ti (8GB) + CUDA 12.6
- **版本控制**：Git + GitHub

---

## 三、已完成功能

### 3.1 地图可视化功能

#### ✅ 交互式地图
- 支持多种底图切换（深色主题、地形图、卫星图、标准地图）
- 自动适配中国空域（中心：35.5°N, 114.5°E）
- 缩放级别：3-19

#### ✅ 航班轨迹显示
- 动态绘制航班轨迹线（Polyline）
- 实时更新航班位置标记
- 悬停显示航班信息（航班号、高度、速度等）

#### ✅ 时间轴控制
- 时间滑块：拖动查看不同时刻的航班状态
- 时间显示：实时显示当前时间点
- 航班统计：显示当前时刻活跃航班数

#### ✅ 播放控制
- 播放/暂停按钮
- 播放速度调节（0.1x - 10x）
- 重置/清除功能

### 3.2 轨迹预测功能

#### ✅ LSTM模型
- **模型结构**：2层LSTM (hidden=64) → FC(64→16) → FC(16→4)
- **输入特征**：纬度、经度、高度、速度、航向（5个特征）
- **输出目标**：下一时刻的纬度、经度、高度、速度（4个目标）
- **序列长度**：10个时间步

#### ✅ 预测API
- **REST接口**：`POST /api/predict`
- **批量预测**：`POST /api/predict/batch`
- **实时预测**：支持1-10步预测

#### ✅ 预测控制
- 预测步数调节（1-10步）
- 预测轨迹可视化（绿色轨迹线）
- 预测状态显示

### 3.3 数据管理功能

#### ✅ ADSB数据加载
- 支持多种数据格式（JSON）
- 自动数据转换与标准化
- 航班去重处理

#### ✅ 数据质量监控
- 实时显示活跃航班数
- 显示最后更新时间
- 显示总轨迹点数
- 数据源状态指示

---

## 四、数据处理流程

### 4.1 数据来源

**ADS-B原始数据**：
- **时间范围**：2025-12-10 ~ 2025-12-17（约一周）
- **空域范围**：中国空域（14°~51°N, 77°~127°E）
- **数据量**：约9.95亿条记录
- **数据格式**：原始TXT → Parquet（解析后）

### 4.2 数据处理流程

```
原始TXT文件
    ↓ 解析与验证
Parsed Parquet（36 GB, 228分区）
    ↓ 质量标记
Clean Parquet（40 GB, 228分区）
    ↓ 抽样导出
地图样本JSON（10架航班, 193 KB）
    ↓ 特征工程
训练数据集（500万序列）
    ↓ LSTM训练
预测模型（best_model.pth）
```

### 4.3 质量标记

**Clean层数据包含11个质量标记字段**：

| 字段 | 类型 | 说明 |
|---|---|---|---|
| `is_valid_position` | int8 | 位置是否有效（纬度/经度范围检查） |
| `is_valid_time` | int8 | 时间是否有效 |
| `is_valid_speed` | int8 | 速度是否有效（0-600节） |
| `is_valid_heading` | int8 | 航向是否有效（0-360°） |
| `is_valid_icao` | int8 | ICAO代码是否有效 |
| `is_ground_point` | int8 | 是否为地面点 |
| `quality_flag` | string | 质量等级（GOOD/WARN/BAD） |
| `quality_reason` | string | 异常原因列表 |
| `track_point_count` | int | 轨迹点数 |
| `track_sparse` | bool | 是否稀疏轨迹（<3点） |
| `track_time_disorder` | int | 时间乱序次数 |

**数据质量统计**：
- GOOD: 99.91%
- WARN: 0.06%
- BAD: 0.03%

---

## 五、模型训练

### 5.1 训练环境

| 项目 | 配置 |
|---|---|
| **服务器** | Linux (Ubuntu 6.1.0-32-amd64) |
| **GPU** | NVIDIA GeForce RTX 4060 Ti (8GB) |
| **CUDA** | 12.6 |
| **Python** | 3.10 (Conda环境 `adsb`) |
| **PyTorch** | 2.6.0+cu124 |

### 5.2 训练配置

| 参数 | 值 |
|---|---|
| **训练序列数** | 5,000,000 |
| **训练/测试集** | 4,000,000 / 1,000,000 (80%/20%) |
| **Epochs** | 50 |
| **Batch size** | 256 |
| **学习率** | 0.001 |
| **优化器** | Adam |
| **损失函数** | MSELoss |

### 5.3 模型评估

| 指标 | 值 |
|---|---|
| **Best Test Loss** | 0.0112 |
| **纬度 MAE** | 0.72° (约80km) |
| **经度 MAE** | 0.73° (约81km) |
| **高度 MAE** | 339米 |
| **速度 MAE** | 4.2节 |

### 5.4 标准化参数

```json
{
  "X_mean": [31.11, 109.49, 8420.04, 212.73, 180.11],
  "X_std": [7.79, 9.42, 3414.13, 52.14, 105.20],
  "y_mean": [31.11, 109.49, 8418.90, 212.71],
  "y_std": [7.79, 9.42, 3415.68, 52.18]
}
```

---

## 六、前端实现

### 6.1 页面结构

```
layouts/
├── _default/
│   ├── baseof.html         # 基础模板
│   ├── home.html           # 首页
│   └── map.html           # 地图页面
└── partials/
    └── map.html           # 地图组件
```

### 6.2 核心JavaScript模块

| 模块 | 功能 |
|---|---|
| **loadADSBData()** | 加载ADS-B数据 |
| **initMap()** | 初始化Leaflet地图 |
| **addFlight()** | 添加航班到地图 |
| **updateTimeline()** | 更新时间轴 |
| **playAnimation()** | 播放动画 |
| **predictFlightTrajectory()** | 预测轨迹 |

### 6.3 样式设计

**深色主题**：
- 主色调：深蓝/黑色 (#1a1a2e, #16213e)
- 强调色：青色 (#06b6d4)、绿色 (#10b981)
- 字体：系统默认字体，支持中文

**响应式布局**：
- 侧边栏：300px宽（左侧）
- 地图容器：自适应宽度
- 移动端适配

---

## 七、后端实现

### 7.1 预测API

**文件**：`model_training/flight_prediction_api.py`

**接口**：

| 方法 | 路径 | 功能 |
|---|---|---|
| GET | `/health` | 健康检查 |
| POST | `/api/predict` | 单航班预测 |
| POST | `/api/predict/batch` | 批量预测 |

**请求格式**：
```json
{
  "trajectory": [
    {"lat": 39.48, "lng": 117.82, "altitude": 10394, "speed": 216.8, "heading": 316.2},
    ...
  ],
  "steps": 5
}
```

**响应格式**：
```json
{
  "predictions": [
    {"lat": 39.72, "lng": 117.52, "altitude": 10386, "speed": 213.2, "heading": 316.0},
    ...
  ],
  "sequence_used": 10,
  "steps_predicted": 5
}
```

### 7.2 模型文件

| 文件 | 大小 | 说明 |
|---|---|---|
| `best_model.pth` | 213 KB | 模型权重（含config和norm_params） |
| `norm_params.json` | 494 B | 标准化参数 |
| `training_log.json` | 3.8 KB | 训练日志 |

---

## 八、遇到的问题与解决方案

### 8.1 模型预测方向错误

**问题描述**：
- v1模型预测的轨迹方向与实际完全相反
- 实际：纬度 39.70→39.82（向北），预测：39.70→38.95（向南）

**原因分析**：
- 训练时使用了float32计算标准化参数，导致精度损失
- X_mean（输入特征均值）与y_mean（输出目标均值）不一致
- X lng_mean = 42.99°（不在正常中国经度范围）

**解决方案**：
- 修改训练脚本，先转float64再计算统计量
- 服务器重新训练模型（v2版本）
- 更新标准化参数验证一致性

**修复后**：
- X lng_mean = 109.49° ✅
- y lng_mean = 109.49° ✅
- 输入输出参数一致 ✅

### 8.2 API特征顺序错误

**问题描述**：
- API代码中特征顺序为 `lng, lat`，与训练时的 `lat, lng` 不一致

**解决方案**：
- 恢复API代码为正确的特征顺序
- 验证预测结果

### 8.3 Hugo部署路径问题

**问题描述**：
- 本地运行时资源路径不正确
- GitHub Pages部署需要特定的baseURL

**解决方案**：
- 本地：`hugo --baseURL=/`
- GitHub：`hugo --baseURL=https://zygty.github.io/web/`
- 在workflow中明确指定baseURL

### 8.4 数据格式兼容性

**问题描述**：
- 服务器新数据格式（sample_flights.json）与本地旧格式不一致

**解决方案**：
- 修改JavaScript支持两种数据格式
- 优先加载新格式数据
- 自动格式转换

---

## 九、项目成果

### 9.1 完成的功能

| 功能模块 | 完成度 | 说明 |
|---|---|---|
| **地图可视化** | ✅ 100% | 多底图、轨迹显示、时间轴控制 |
| **播放控制** | ✅ 100% | 播放/暂停、速度调节、重置 |
| **数据监控** | ✅ 100% | 航班数、数据状态、统计信息 |
| **轨迹预测** | ✅ 80% | LSTM模型、API接口、预测可视化 |
| **数据处理** | ✅ 100% | 解析、清洗、质量标记 |
| **模型训练** | ✅ 90% | LSTM训练、评估、优化 |

### 9.2 代码统计

| 类型 | 文件数 | 代码行数 |
|---|---|---|
| **JavaScript** | 2 | ~800行 |
| **Python（后端）** | 4 | ~600行 |
| **Python（训练）** | 5 | ~800行 |
| **HTML/CSS** | 6 | ~1000行 |
| **配置文件** | 3 | ~50行 |

### 9.3 数据规模

| 数据类型 | 大小/数量 |
|---|---|
| 原始ADS-B数据 | 9.95亿条记录 |
| Clean层数据 | 40 GB (228分区) |
| 地图样本 | 193 KB (10架航班) |
| 训练序列 | 500万序列 |

---

## 十、部署方式

### 10.1 本地运行

```bash
# 启动Hugo服务器
cd ~/Desktop/web
hugo server --port 1313

# 访问
http://localhost:1313/web/
```

### 10.2 GitHub Pages部署

**自动化部署**：
- 推送代码到main分支
- GitHub Actions自动构建
- 部署到GitHub Pages

**访问地址**：https://zygty.github.io/web/

### 10.3 服务器部署

```bash
# 启动预测API
cd ~/Desktop/web/model_training
python3 flight_prediction_api.py

# 访问API
http://localhost:5001/health
```

---

## 十一、未来改进方向

### 11.1 短期改进

1. **提高预测精度**
   - 增大模型容量（hidden_size=128或更大）
   - 使用更多训练数据（全量1.9亿序列）
   - 添加时间间隔等衍生特征

2. **优化前端体验**
   - 添加航班搜索功能
   - 支持多航班同时预测
   - 添加预测置信度显示

3. **数据接入**
   - 接入实时ADS-B数据流
   - WebSocket实时更新
   - 支持历史数据回放

### 11.2 长期规划

1. **模型升级**
   - 尝试Transformer架构替代LSTM
   - 多任务学习（同时预测多个航班）
   - 航线意图识别

2. **功能扩展**
   - 机场进出港分析
   - 空域拥堵热力图
   - 异常行为检测

3. **性能优化**
   - 前端数据分页加载
   - 后端缓存机制
   - GPU加速推理

---

## 十二、项目文件结构

```
web/
├── content/                    # Hugo内容目录
│   ├── _index.md              # 首页
│   ├── map.md                 # 地图页面
│   └── Assignment*.md         # 作业文档
├── layouts/                    # Hugo模板
│   ├── _default/
│   │   ├── baseof.html        # 基础模板
│   │   ├── home.html          # 首页模板
│   │   └── map.html          # 地图模板
│   └── partials/
│       └── map.html          # 地图组件
├── static/                     # 静态资源
│   ├── css/
│   │   ├── flight-tracker.css # 地图样式
│   │   └── style.css         # 通用样式
│   ├── js/
│   │   └── flight-tracker.js # 地图逻辑
│   └── data/
│       ├── sample_flights.json    # 地图样本数据
│       ├── adsb_flights_combined_simplified.json
│       └── adsb_flights_cleaned.json
├── model_training/             # 模型相关
│   ├── flight_lstm_pytorch.py   # LSTM模型定义
│   ├── flight_prediction_api.py  # 预测API
│   ├── best_model.pth           # 模型权重
│   ├── norm_params.json         # 标准化参数
│   └── training_log.json        # 训练日志
├── adsb_server_output/          # 服务器输出
│   ├── model/                    # 训练好的模型
│   ├── reports/                  # 质量报告
│   ├── map_data/                 # 地图样本
│   ├── duihua.md                 # 服务器对话记录
│   └── adsb_data_cleaning_training_report.md
├── hugo.toml                    # Hugo配置
└── .github/workflows/
    └── hugo.yml                 # GitHub Actions配置
```

---

## 十三、技术亮点

### 13.1 前端技术亮点

1. **模块化设计**：功能分离，易于维护
2. **响应式布局**：支持多种屏幕尺寸
3. **多底图支持**：提供多种地图样式选择
4. **时间轴交互**：直观的时间控制体验

### 13.2 后端技术亮点

1. **流式数据处理**：避免内存溢出，支持大规模数据
2. **RESTful API设计**：接口清晰，易于调用
3. **模型内嵌配置**：模型文件包含所有必要参数

### 13.3 数据处理亮点

1. **质量标记系统**：11个质量字段，全面评估数据质量
2. **分层架构**：Parsed → Clean → Model，流程清晰
3. **自动化流程**：一键完成解析→清洗→训练

---

## 十四、总结

本项目成功实现了一个**航空轨迹可视化与预测系统**，具有以下特点：

✅ **完整的技术栈**：从前端可视化到后端API，从数据处理到模型训练  
✅ **大规模数据处理**：处理近10亿条ADS-B数据，生成500万训练序列  
✅ **深度学习应用**：使用LSTM进行轨迹预测，MAE达到0.72°  
✅ **现代化UI**：深色主题、响应式设计、交互式地图  
✅ **可扩展架构**：预留实时数据接入接口，支持多种部署方式  

该项目展示了从**数据采集→处理→分析→可视化→预测**的完整数据科学流程，是一个综合性的Web应用开发项目。

---

**项目地址**：http://localhost:1313/web/  
**GitHub仓库**：https://github.com/zygty/web  
**作者**：李浩轩  
**时间**：2026年6月
